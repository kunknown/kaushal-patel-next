export enum THEME {
  light = "light",
  dark = "dark",
}

export enum MONSTER_ARENA_VIEWS {
  ARENA = "ARENA",
  BATTLE_RECORD = "BATTLE_RECORD",
}

export enum MONSTER_ARENA_STEPS {
  SELECTION = "SELECTION",
  BATTLE = "BATTLE",
  HISTORY = "HISTORY",
}
